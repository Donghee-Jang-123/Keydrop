spring:
  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

app:
  media:
    dir: "D:/KeydropMedia"
    urlPrefix: "/media"